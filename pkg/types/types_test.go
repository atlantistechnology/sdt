package types_test

/* This package only contains some struct defs, some enums, some values of
   those structs, and a mapping of strings to a struct type.  I.e. no funcs.
   Everything availble to test is alredy handled by the compiler and other
   packages that utilize these items
*/
